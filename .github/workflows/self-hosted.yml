name: Self Hosted Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Run command
        run: |
          echo "Runner is working"
          hostname
          pwd
